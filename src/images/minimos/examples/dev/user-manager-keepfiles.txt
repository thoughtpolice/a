# SPDX-FileCopyrightText: © 2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0
#
# Two files the base denies that a dev machine needs back.
# systemd-user-runtime-dir creates /run/user/<uid>, and user@.service
# binds to it. loginctl shows what logind knows. Everything else
# user@1000 needs is already in the base.

/usr/lib/systemd/systemd-user-runtime-dir
/usr/bin/loginctl
