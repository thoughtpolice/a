# SPDX-FileCopyrightText: © 2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0
#
# glibc is in the apk list for its tools, but its loader tools stay
# culled, same as the base: .so resolution happens at build time and
# ldconfig.service is masked.

/usr/bin/ldconfig
/usr/bin/ld.so

# The base ships these three terminfo entries itself, and a second copy
# would replace a base path, which the composition policy refuses.
/etc/terminfo/d/dumb
/etc/terminfo/l/linux
/etc/terminfo/x/xterm-256color
