# SPDX-FileCopyrightText: © 2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0
#
# Every program the dev tool packages install in /usr/bin, glibc's
# getent, ldd and iconv included. cull.py adds the libraries they link,
# except those the base already ships.

/usr/bin/*

# git's machinery lives outside /usr/bin.
/usr/libexec/git-core/**
/usr/share/git-core/**

# The rest of the terminfo set. SSH sessions arrive with whatever TERM
# the client uses, and less, top and watch want the matching entry. The
# base keeps three entries, and the denyfile leaves those to it.
/etc/terminfo/**
