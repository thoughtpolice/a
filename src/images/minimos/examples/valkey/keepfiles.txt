# SPDX-FileCopyrightText: © 2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0
#
# Allowlist culling the valkey packages. valkey-sentinel and the
# valkey-check-* names are symlinks onto the multi-call valkey-server
# binary, so the glob keeps the whole family in one line; the .so
# closure is resolved automatically by cull.py.
#
# valkey-cli rides along deliberately: the image has no coreutils, so
# the cli doubles as the in-VM verification tool (`valkey-cli ping`).

/usr/bin/valkey-*
