root:x:0:
daemon:x:1:
bin:x:2:
sys:x:3:
tty:x:5:
disk:x:6:
adm:x:4:
# exedev is a member so day-to-day/agent SSH sessions can read the
# whole journal (single-tenant VM; the user is the admin).
systemd-journal:x:105:exedev
messagebus:x:106:
# chronyd runs as this account and reads /dev/ptp0 through this group;
# the unit chowns the device to it at start, so no one else can.
chrony:x:107:
exedev:x:1000:
nogroup:x:65534:
