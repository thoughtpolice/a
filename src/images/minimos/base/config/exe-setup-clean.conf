# SPDX-FileCopyrightText: © 2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0
#
# Processed by exe-setup.service's ExecStartPost via
# `systemd-tmpfiles --remove <this file>`. Deliberately NOT installed
# under tmpfiles.d/: the boot-time `systemd-tmpfiles --create --remove`
# pass would delete the setup script before it ever ran.
r /exe.dev/setup
