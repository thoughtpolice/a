# SPDX-FileCopyrightText: © 2024-2026 Austin Seipp
# SPDX-License-Identifier: Apache-2.0

# All select() logic and target creation lives in defs.bzl because the cellar
# cell's noprelude shim overrides the built-in select() in BUILD files.

load(":defs.bzl", "create_all")

create_all()
